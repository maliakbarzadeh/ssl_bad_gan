# مستندات مدل GAN برای دیتاست Two Circles

## 📋 توضیحات کلی

این پروژه یک مدل **Semi-Supervised GAN** برای یادگیری توزیع داده‌های دو دایره متحدالمرکز (Two Circles) است. این مدل از تعداد محدودی داده برچسب‌دار و تعداد زیادی داده بدون برچسب برای آموزش استفاده می‌کند.

### ⭐ ویژگی کلیدی: شناسایی داده‌های Fake به عنوان کلاس جداگانه

طبق مقاله **"Improved Techniques for Training GANs"** (Salimans et al., 2016)، Discriminator به جای اینکه فقط داده‌های واقعی و تولیدی را تفکیک کند، از یک معماری **K+1 کلاسه** استفاده می‌کند:

- **K کلاس**: برای داده‌های واقعی (دایره داخلی و بیرونی)
- **1 کلاس اضافی**: برای شناسایی داده‌های تولیدی (Fake)

این روش باعث می‌شود:
1. ✅ Discriminator داده‌های تولیدی را به عنوان کلاس جداگانه شناسایی کند
2. ✅ Generator مجبور شود داده‌هایی تولید کند که در مرز بین کلاس‌های واقعی قرار گیرند
3. ✅ هم‌پوشانی کمتری با داده‌های واقعی داشته باشند

## 🎯 هدف

- **یادگیری توزیع داده**: مدل باید بتواند نقاط 2D را در الگوی دو دایره متحدالمرکز تولید کند
- **طبقه‌بندی با داده کم**: با استفاده از تنها 40 نمونه برچسب‌دار (20 نمونه برای هر کلاس)
- **استفاده از داده بدون برچسب**: برای بهبود عملکرد از 2000 نمونه بدون برچسب استفاده می‌شود

## 🗂️ ساختار فایل‌ها

### فایل‌های اصلی
- **`two_circles_trainer.py`**: فایل اصلی آموزش مدل
- **`two_circles_model.py`**: معماری شبکه‌های عصبی (Generator, Discriminator, Encoder)
- **`data.py`**: بارگذاری و مدیریت دیتاست
- **`config.py`**: تنظیمات و پارامترهای مدل
- **`utils.py`**: توابع کمکی (log_sum_exp, entropy, و غیره)

### فایل‌های خروجی
- **`two_circles_log/`**: پوشه شامل لاگ‌ها و تصاویر
  - `two_circles.FM+PT+ENT.run0.txt`: فایل لاگ آموزش
  - `two_circles.FM+PT+Ent.run0.png`: تصویر ساده (Real vs Generated)
  - `two_circles.detailed.iter_*.png`: تصاویر دقیق با پیش‌بینی‌های discriminator

## ⚙️ پارامترهای مدل

### تنظیمات دیتاست
```python
dataset = 'two_circles'
n_samples = 2000              # تعداد کل نمونه‌ها
noise_level = 0.05            # میزان نویز در داده‌ها
factor = 0.5                  # نسبت شعاع دایره داخلی به بیرونی
size_labeled_data = 40        # تعداد داده‌های برچسب‌دار (20 نمونه از هر کلاس)
```

### تنظیمات شبکه
```python
image_size = 2                # ابعاد ورودی (x, y)
num_label = 2                 # تعداد کلاس‌های واقعی (دایره داخلی و بیرونی)
num_classes = 3               # کل کلاس‌ها شامل Fake (inner=0, outer=1, fake=2)
noise_size = 20               # ابعاد فضای latent برای Generator
```

### تنظیمات آموزش
```python
max_epochs = 5000             # حداکثر تعداد epoch
train_batch_size = 32         # اندازه batch برای آموزش
dis_lr = 0.003               # learning rate برای Discriminator
gen_lr = 0.001               # learning rate برای Generator
```

### پارامترهای Loss
```python
ent_weight = 0.1             # وزن Conditional Entropy Loss
pt_weight = 0.5              # وزن Pull-away Term Loss
```

### تنظیمات نمایش
```python
eval_period = 200            # هر 200 iteration ارزیابی انجام شود
vis_period = 100             # هر 100 iteration تصویر ساده ذخیره شود
plot_period = 50             # هر 50 iteration تصویر دقیق ذخیره شود
```

## 🏗️ معماری مدل

### 1. Discriminator (متمایزکننده)
```
Input (2D) → Linear(2, 128) → ReLU → Dropout(0.3)
           → Linear(128, 128) → ReLU → Dropout(0.3)
           → Linear(128, 128) → ReLU
           → Linear(128, 3) → Output (3 Logits)
```

**خروجی**: 3 کلاس
- **کلاس 0**: دایره داخلی (Inner Circle)
- **کلاس 1**: دایره بیرونی (Outer Circle)
- **کلاس 2**: داده تولیدی (Fake/Generated)

**نقش**: 
- تشخیص داده واقعی از داده تولیدی
- طبقه‌بندی داده‌های واقعی به دایره داخلی (0) یا بیرونی (1)
- شناسایی داده‌های تولیدی به عنوان کلاس جداگانه (2)

### 2. Generator (تولیدکننده)
```
Input (Noise: 20D) → Linear(20, 128) → BatchNorm → ReLU
                   → Linear(128, 128) → BatchNorm → ReLU
                   → Linear(128, 2) → Tanh → Output (2D)
```

**نقش**: تولید نقاط 2D جدید که شبیه توزیع دو دایره باشند

### 3. Encoder (کدگذار)
```
Input (2D) → Linear(2, 128) → BatchNorm → ReLU
           → Linear(128, 128) → BatchNorm → ReLU
           → Linear(128, 20) → Output (Noise)
```

**نقش**: نگاشت نقاط 2D به فضای latent (برای استفاده در روش‌های پیشرفته‌تر)

## 📊 توابع Loss

### 1. Supervised Loss (Classification)
```python
lab_loss = CrossEntropyLoss(predictions[:, :K], true_labels)
```
محاسبه می‌شود فقط روی **داده‌های برچسب‌دار** و فقط با استفاده از **K کلاس واقعی** (کلاس Fake استفاده نمی‌شود)

### 2. Unsupervised Loss (GAN)

#### الف) Real Data Loss
```python
unl_logsumexp = log_sum_exp(unl_logits[:, :K])  # فقط روی K کلاس واقعی
true_loss = -mean(unl_logsumexp) + mean(softplus(unl_logsumexp))
```
داده‌های واقعی نباید به کلاس Fake نسبت داده شوند

#### ب) Fake Data Loss
```python
fake_loss = CrossEntropyLoss(gen_logits, fake_class_label)  # Label = K
```
داده‌های تولیدی **باید** به کلاس K (Fake) نسبت داده شوند

### 3. Conditional Entropy Loss
```python
unl_probs = softmax(unl_logits[:, :K])  # فقط روی K کلاس واقعی
ent_loss = ent_weight * mean(sum(unl_probs * log(unl_probs)))
```
باعث می‌شود مدل روی داده‌های بدون برچسب اطمینان بیشتری داشته باشد (فقط برای کلاس‌های واقعی)

### 4. Feature Matching Loss
```python
fm_loss = mean(|mean(gen_features) - mean(real_features)|)
```
Generator تلاش می‌کند داده‌هایی تولید کند که feature های آن‌ها شبیه داده‌های واقعی باشد

### 5. Pull-away Term Loss
```python
cosine = normalize(gen_features) @ normalize(gen_features).T
pt_loss = pt_weight * sum((cosine * mask)²) / (n * (n-1))
```
باعث تنوع در داده‌های تولیدی می‌شود (جلوگیری از mode collapse)

## 🎨 نمودارها و تصاویر خروجی

### تصویر ساده (هر 100 iteration)
**فایل**: `two_circles.FM+PT+Ent.run0.png`

دو نمودار جنب هم:
- **سمت چپ - داده واقعی**:
  - ⚪ خاکستری: داده‌های واقعی (بدون تفکیک برچسب)
  
- **سمت راست - داده تولیدی**:
  - 🔴 قرمز: داده‌های تولید شده توسط Generator

### تصویر دقیق (هر 50 iteration)
**فایل**: `two_circles.detailed.iter_XXX.png`

دو نمودار جنب هم با جزئیات کامل:

#### نمودار سمت چپ: Ground Truth (واقعیت)
| رنگ | نوع داده | توضیحات |
|-----|---------|---------|
| 🔵 آبی | داده برچسب‌دار دایره داخلی | دایره پر با border مشکی |
| 🟠 نارنجی | داده برچسب‌دار دایره بیرونی | دایره پر با border مشکی |
| ⚪ خاکستری | داده بدون برچسب | نقاط کوچک بدون border |
| 🔴 قرمز | داده تولیدی | نقاط کوچک بدون border |

#### نمودار سمت راست: Discriminator Predictions (پیش‌بینی‌ها)
| رنگ | نوع داده | توضیحات |
|-----|---------|---------|
| 🟢 سبز | داده برچسب‌دار صحیح | پیش‌بینی درست - دایره با border مشکی |
| ❌ قرمز | داده برچسب‌دار اشتباه | پیش‌بینی غلط - علامت X بزرگ |
| 🔷 آبی روشن | داده بدون برچسب (پیش‌بینی: داخلی) | Discriminator آن را کلاس 0 تشخیص داده |
| 🟡 گندمی | داده بدون برچسب (پیش‌بینی: بیرونی) | Discriminator آن را کلاس 1 تشخیص داده |
| � بنفش | داده بدون برچسب (پیش‌بینی: Fake) | Discriminator اشتباهاً آن را Fake تشخیص داده! |
| 🔹 آبی تیره | داده تولیدی (پیش‌بینی: داخلی) | **اشتباه** - نباید به کلاس واقعی نسبت داده شود |
| 🟠 نارنجی تیره | داده تولیدی (پیش‌بینی: بیرونی) | **اشتباه** - نباید به کلاس واقعی نسبت داده شود |
| ⭐ سبز لیمویی | داده تولیدی (پیش‌بینی: Fake) | **صحیح** - به درستی به کلاس Fake نسبت داده شده (ستاره) |

**عنوان نمودار راست شامل**:
- شماره Iteration
- دقت روی داده‌های برچسب‌دار: `XX.X% (تعداد صحیح / کل)`
- نرخ تشخیص Fake: `XX.X%` (درصد داده‌های تولیدی که به درستی به کلاس Fake نسبت داده شده‌اند)

## 📈 خروجی لاگ

فایل لاگ شامل اطلاعات زیر است:

### اطلاعات اولیه
```
Using device: cuda
GPU: NVIDIA GeForce RTX 3080
seed : 13
dataset : two_circles
...
```

### اطلاعات هر Iteration ارزیابی (هر 200 iteration)
```
#200    train: 0.1234, 0.0250 | dev: 0.1567, 0.0300 | best: 0.0300
        | lab acc: 0.9500 | unl acc: 0.8500 | gen acc: 0.7200
        | lab loss: 0.2100 | unl loss: 0.3400 | ent loss: 0.0120
        | fm loss: 0.0450 | pt loss: 0.0023
        | [Eval] unl acc: 0.8900, gen acc: 0.7500, max unl acc: 0.9200, max gen acc: 0.7800
        | lr: 0.00300
```

**شرح پارامترها**:
- `train`: loss و error rate روی داده‌های برچسب‌دار
- `dev`: loss و error rate روی داده‌های تست
- `best`: بهترین error rate تاکنون
- `lab acc`: دقت طبقه‌بندی روی داده‌های برچسب‌دار (باید نزدیک 1 باشد)
- `unl acc`: درصد داده‌های واقعی که به کلاس‌های واقعی (0 یا 1) نسبت داده شده‌اند (نه Fake)
- `gen acc`: **درصد داده‌های تولیدی که به درستی به کلاس Fake نسبت داده شده‌اند** (باید به 1 نزدیک شود)
- `lab loss`: Classification loss
- `unl loss`: GAN loss (شامل true_loss و fake_loss)
- `ent loss`: Entropy loss
- `fm loss`: Feature Matching loss
- `pt loss`: Pull-away Term loss
- `lr`: Learning rate فعلی

### اطلاعات ویژوالیزیشن (هر 50 iteration)
```
[Visualization 50] Labeled Data Accuracy: 87.50%
[Visualization 100] Labeled Data Accuracy: 92.50%
[Visualization 150] Labeled Data Accuracy: 95.00%
```

## 🚀 نحوه اجرا

### 1. فعال‌سازی محیط مجازی
```powershell
cd D:\GAN\ssl_bad_gan
..\venv3.12\Scripts\Activate.ps1
```

### 2. اجرای آموزش
```powershell
python two_circles_trainer.py -suffix run0
```

### 3. پارامترهای اختیاری
```powershell
python two_circles_trainer.py -suffix run1
```
`-suffix`: شناسه برای تمایز بین اجراهای مختلف

## 💻 نیازمندی‌های سخت‌افزاری

- **GPU**: اختیاری (کد به صورت خودکار GPU یا CPU را تشخیص می‌دهد)
- **RAM**: حداقل 4GB
- **فضای دیسک**: حدود 100MB برای ذخیره تصاویر و لاگ‌ها

## 📦 کتابخانه‌های مورد نیاز

```
torch >= 1.8.0
torchvision
numpy
matplotlib
scikit-learn
```

## 🔍 نکات مهم

### 1. معماری K+1 کلاسه
**این مهمترین ویژگی است!**

در این پیاده‌سازی، Discriminator **3 کلاس** دارد:
- **کلاس 0**: دایره داخلی (Inner Circle) - داده واقعی
- **کلاس 1**: دایره بیرونی (Outer Circle) - داده واقعی  
- **کلاس 2**: داده تولیدی (Fake/Generated)

**چرا این مهم است؟**
1. ✅ Generator مجبور می‌شود داده‌هایی تولید کند که در **مرز بین دو کلاس واقعی** قرار گیرند
2. ✅ Discriminator نمی‌تواند به راحتی داده تولیدی را به یکی از کلاس‌های واقعی نسبت دهد
3. ✅ هم‌پوشانی کمتری بین داده‌های واقعی و تولیدی وجود دارد
4. ✅ مدل Semi-Supervised بهتر کار می‌کند

### 2. نحوه محاسبه Loss
- **برای داده‌های برچسب‌دار**: فقط از 2 کلاس اول استفاده می‌شود (Fake class نادیده گرفته می‌شود)
- **برای داده‌های واقعی بدون برچسب**: باید به یکی از 2 کلاس اول نسبت داده شوند (نه Fake)
- **برای داده‌های تولیدی**: باید حتماً به کلاس Fake (کلاس 2) نسبت داده شوند

### 3. معیار موفقیت
- `lab_acc` باید به بالای 90% برسد (دقت طبقه‌بندی)
- `unl_acc` باید به بالای 85% برسد (داده واقعی به عنوان real شناخته شود)
- `gen_acc` باید به بالای 70% برسد (داده تولیدی به عنوان fake شناخته شود)

### 4. تشخیص GPU/CPU
کد به صورت خودکار GPU را تشخیص می‌دهد:
```python
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
```

### 2. Learning Rate Decay
Learning rate به صورت خطی کاهش می‌یابد:
```python
lr = initial_lr * min(3 * (1 - epoch_ratio), 1)
```

### 2. داده‌های برچسب‌دار
- فقط 40 نمونه از 2000 نمونه برچسب دارند (2%)
- 20 نمونه از کلاس 0 (دایره داخلی)
- 20 نمونه از کلاس 1 (دایره بیرونی)

### 3. نحوه تولید داده
داده‌ها با استفاده از `sklearn.datasets.make_circles` تولید می‌شوند:
- `n_samples`: 2000 نمونه برای train، 1000 نمونه برای test
- `noise`: 0.05 (میزان نویز)
- `factor`: 0.5 (نسبت شعاع دایره داخلی به بیرونی)

### 4. هدف آموزش
- **Generator**: تولید نقاط 2D که:
  - پراکندگی شبیه دو دایره متحدالمرکز داشته باشند
  - در مرز بین دو کلاس قرار گیرند (نه دقیقاً روی یکی از دایره‌ها)
- **Discriminator**: 
  - تشخیص داده واقعی از تولیدی (کلاس Fake)
  - طبقه‌بندی دقیق داده‌های واقعی با تعداد کم داده برچسب‌دار
  - شناسایی داده‌های تولیدی به عنوان کلاس جداگانه

### 5. تفاوت با پیاده‌سازی‌های معمولی GAN

## 📉 معیارهای موفقیت

### 1. دقت طبقه‌بندی
- روی داده‌های برچسب‌دار: باید به بالای 90% برسد
- روی داده‌های تست: باید به بالای 85% برسد

### 2. کیفیت تولید و تشخیص Fake
- داده‌های تولیدی باید در مرز بین دو دایره متحدالمرکز قرار گیرند
- تنوع در داده‌های تولیدی (هر دو دایره پوشش داده شوند)
- **مهم**: Discriminator باید حداقل 70% از داده‌های تولیدی را به عنوان Fake تشخیص دهد

### 3. توازن GAN
- `lab_acc` باید به تدریج به 0.9-1.0 برسد
- `unl_acc` باید به تدریج به 0.8-0.95 برسد (داده واقعی به عنوان real)
- `gen_acc` باید از 0 شروع کند و به 0.7-0.9 برسد (داده تولیدی به عنوان fake)

### 4. موقعیت داده‌های تولیدی
در نمودارها، داده‌های تولیدی باید:
- ✅ در مرز بین دو دایره ظاهر شوند
- ✅ بیشتر به رنگ سبز لیمویی (⭐ Fake class) باشند
- ❌ نباید همپوشانی زیادی با دایره‌های اصلی داشته باشند

## 🐛 عیب‌یابی

### مشکل: GPU پیدا نمی‌شود
**راه‌حل**: کد به صورت خودکار روی CPU اجرا می‌شود. برای استفاده از GPU، CUDA و PyTorch با پشتیبانی CUDA نصب کنید.

### مشکل: Mode Collapse (Generator فقط یک نوع داده تولید می‌کند)
**راه‌حل**: افزایش `pt_weight` در config

### مشکل: دقت پایین طبقه‌بندی
**راه‌حل**: 
- افزایش `max_epochs`
- کاهش `learning_rate`
- افزایش `size_labeled_data`

### مشکل: Generator داده‌های روی دایره‌های اصلی تولید می‌کند (نه در مرز)
**راه‌حل**:
- این رفتار **اشتباه** است! بررسی کنید که `gen_acc` (نرخ تشخیص Fake) به بالای 70% می‌رسد
- اگر `gen_acc` پایین است، وزن `fake_loss` را افزایش دهید
- بررسی کنید که Discriminator واقعاً 3 کلاس دارد (نه 2)

### مشکل: داده‌های واقعی به عنوان Fake شناسایی می‌شوند
**راه‌حل**:
- بررسی کنید که `unl_acc` بالای 85% است
- کاهش learning rate
- افزایش تعداد داده‌های برچسب‌دار

## 📚 مراجع

این کد بر اساس مقاله زیر پیاده‌سازی شده:
- "Improved Techniques for Training GANs" (Salimans et al., 2016)
- Feature Matching و Pull-away Term از این مقاله الهام گرفته شده‌اند

## 📝 یادداشت‌های توسعه

### تفاوت با MNIST
- بدون PixelCNN (چون تصویر نیست)
- معماری ساده‌تر (MLP به جای CNN)
- داده 2D به جای تصاویر 28×28

### تفاوت با دیتاست‌های دیگر
- بدون Encoder در loss function (اگرچه معماری موجود است)
- روی داده‌های 2D به جای تصاویر RGB
- **دارای کلاس جداگانه Fake** (3 کلاس: inner, outer, fake)

## ✅ خلاصه

این پروژه نمونه‌ای از **Semi-Supervised Learning** با GAN است که:
- با تعداد بسیار کم داده برچسب‌دار (40 نمونه) آموزش می‌بیند
- از داده‌های بدون برچسب (2000 نمونه) برای بهبود استفاده می‌کند
- **از معماری K+1 کلاسه استفاده می‌کند** (کلاس جداگانه برای Fake)
- قادر به تولید داده‌های جدید در مرز بین کلاس‌ها است
- داده‌های تولیدی را به عنوان کلاس جداگانه شناسایی می‌کند
- دقت طبقه‌بندی بالایی دارد (>90%)
- نمودارهای دقیق و واضح برای درک عملکرد مدل ارائه می‌دهد

**نکته مهم**: هدف اصلی این است که Generator داده‌هایی تولید کند که Discriminator آن‌ها را به عنوان **کلاس Fake شناسایی کند** (نه اینکه آن‌ها را با کلاس‌های واقعی اشتباه بگیرد). این باعث می‌شود داده‌های تولیدی در مرز بین کلاس‌ها قرار گیرند و Semi-Supervised Learning بهتر کار کند.
